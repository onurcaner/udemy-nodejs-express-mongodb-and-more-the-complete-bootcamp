import { ROOT } from '../constants/ROOT';

const dataFolder = `${ROOT}/dev-data/data`;

export const dataPaths = {
  reviews: `${dataFolder}/reviews.json`,
  toursSimple: `${dataFolder}/tours-simple.json`,
  tours: `${dataFolder}/tours.json`,
  users: `${dataFolder}/users.json`,
};
